# SinX Platform 开发计划

> 当前分支：`dev`
>
> 更新时间：2026-09-18（同步至 `dev` 分支最新提交）

## 1. 目标

使用 Java 21、Spring Boot、React、PostgreSQL 和 Redis重新实现 Xboard的核心
业务能力，不继承 Laravel代码、旧数据库、用户页面和用户 API。

核心目标：

- 建立独立的用户网站、用户 API和前端资源体系，消除 Xboard默认应用指纹。
- 重新实现用户、套餐、订单、支付、订阅、节点、流量和支持系统。
- 管理员端参考原版 Xboard的功能，允许保留管理员 API路径和操作语义。
- 节点控制面兼容现有 `xboard-node`，用户代理流量仍直接访问节点 VPS。
- 用户站点支持 HTTP/3、HTTP/2和 HTTP/1.1协商与回退。

## 2. 总体架构

```text
                         Internet
                            │
          ┌─────────────────┴─────────────────┐
          │                                   │
       用户域名                            管理员域名
          │                                   │
     用户 React前端                      管理员 React前端
          │                                   │
     用户侧后端 API                      管理员侧后端 API
          └─────────────────┬─────────────────┘
                            │
                 Spring Boot模块化单体
                            │
        ┌───────────────────┼───────────────────┐
        │                   │                   │
    PostgreSQL            Redis             后台任务

订阅客户端 ─────→ 用户订阅接口 ─────→ 配置输出

xboard-node ─────→ 节点控制 HTTP API / WebSocket

代理客户端 ─────→ 节点 VPS代理端口 ─────→ Internet
                 不经过网站和面板
```

系统分为四个边界：

- 用户侧：公网用户页面和业务接口，必须使用全新的协议与资源特征。
- 管理员侧：仅管理员访问，优先复刻 Xboard已有管理能力。
- 订阅侧：使用独立域名和不透明凭据返回客户端配置。
- 节点侧：控制数据连接面板，用户代理数据直接经过节点 VPS。

## 3. 用户侧开发

### 3.1 用户侧后端

需要实现的接口领域：

- 身份：注册、登录、退出、令牌刷新、邮件验证、密码重置。
- 账户：用户资料、设备会话、安全设置、订阅状态。
- 商品：套餐查询、价格周期、试用和套餐变更规则。
- 交易：订单创建、支付、取消、续费和支付回调。
- 营销：优惠券、邀请关系和佣金（礼品卡不做）。
- 订阅：订阅凭据、客户端识别、配置输出和凭据重置。
- 支持：公告、知识库和通知（工单已放弃，改由外部客服系统替代，见 §10.2 D）。
- 用量：流量、有效期、设备限制和使用记录。

已经完成：

- [x] 注册前邮箱验证码、Cloudflare Turnstile和单 IP限流
- [x] 注册、登录、刷新和退出后端流程
- [x] 新注册邮箱在写库前完成验证
- [x] 密码找回、密码修改和账户资料
- [x] Access/Refresh Token轮换与重放检测
- [x] 设备会话管理
- [x] USER/ADMIN独立会话、Token audience、Refresh Cookie和有效期
- [x] 邀请码生成、查询、注册填写和邀请人关系（策略由管理员配置）
- [x] 套餐目录、价格周期、容量和可售状态查询
- [x] 当前订阅权益、流量用量、有效期和限制查询

仍需完成：

- [x] 下单：套餐详情/结算页、付款周期选择、优惠券校验、订单折抵计算
  （优惠券 → 套餐升级折抵 → 余额折抵，口径对齐原版）、创建与取消订单
- [x] 支付：易支付（EPay）协议全链路 —— 后台配置、发起支付、回调自动开通。
  与 BEpusdt 加密货币网关共用同一协议，区别只是多配一条 EPay 实例
- [ ] 优惠券管理页、佣金。**其余支付网关与退款不做**（只做 EPay，见 §10.2 D）；礼品卡不做
- [x] 订阅凭据与配置输出：`users.subscription_token` 明文能力 URL、`GET /sub/{token}`
  公开入口、Clash/Clash.Meta/Stash/Surge/Surfboard/sing-box/通用 v2ray 链接七种输出、
  管理员可编辑模板、自助轮换（见 §5）
- [ ] 公告、知识库和通知后端（工单已放弃，见 §10.2 D）
- [ ] 用户流量使用记录与自动重置任务（节点流量上报已接入权益计费，见 §5）

### 3.2 用户 API 指纹设计

用户侧是应用指纹重设计的重点，不能保留以下 Xboard默认特征：

- `/api/v1/*`、`/api/v2/*`和 `/s/{token}`等路径模式。
- `status/message/data/error`等固定响应包装。
- Xboard错误码、分页字段、请求参数和客户端识别方式。
- Laravel、Nginx、Spring Boot或其他框架默认响应头和错误页面。
- 原版静态资源、文件名称、页面结构、文案和资源哈希。

新用户接口按使用场景拆分：

- 浏览器业务查询和修改使用新的 GraphQL契约。
- 登录、刷新、退出和安全操作使用独立会话接口。
- 支付回调使用独立且可验证的 Webhook接口。
- 订阅使用独立域名、不透明凭据和新的入口格式。
- 文件下载使用独立文件接口。

指纹控制要求：

- 用户接口拥有自己的字段、错误结构、分页方式和状态语义。
- 生产环境隐藏框架版本、默认错误页和非必要响应头。
- 前端只调用用户域名允许的接口，不复用管理员或节点接口。
- API、静态资源、订阅和节点控制使用不同的安全策略与限流规则。
- 保持协议稳定和可维护，不通过频繁随机修改接口制造兼容问题。

已按此落地的实现边界：

- 用户业务查询走 `POST /gateway` GraphQL（`offerCatalog`、`viewer`、
  `viewerEntitlement`、`deviceSessions`、`revokeDeviceSession`）。
  匿名可读的只有 `systemStatus` 与 `siteName`（站点名称用于客户端深链备注名，
  本就会出现在订阅配置里）。
- 登录、刷新、退出和安全操作走 `/session/*` 独立会话接口。
- 管理员控制面沿用 `/api/v2/admin/*` 与 `/control/*`，节点兼容层沿用
  `/api/v2/server`、`/api/v1/server/UniProxy` 与 `/ws`，不与用户接口混用。
- 隐藏框架版本、默认错误页和非必要响应头属上线验收项（见阶段八）。

### 3.3 用户侧前端

需要实现的页面：

- 注册、登录、邮件验证和密码重置。
- 用户首页、订阅状态和流量信息。
- 套餐、下单、支付和订单记录。
- 订阅获取、客户端下载和使用引导。
- 个人资料、安全设置和登录设备。
- 优惠券、邀请与佣金（礼品卡不做）。
- 公告和知识库。

当前状态：

- [x] 直接复用原版 Freedom HTML及其静态资源作为公共主页
- [x] 注册页面
- [x] 注册邮箱验证码、Turnstile二次校验和安全状态反馈
- [x] 登录页面
- [x] 注册前邮箱验证闭环（写库前完成验证；无独立“确认页面”路由）
- [x] 密码找回与重置页面
- [x] 账户概览与资料修改页面
- [x] 登录密码修改页面
- [x] 登录设备页面
- [x] 页面切换保持登录状态
- [x] 套餐展示与价格周期页面（已接入 `offerCatalog`，区分周期订阅与一次性流量包）
- [ ] 订阅分类导航：全部 / 按周期 / 按流量三个页签（原版用户站有，当前是
  平铺列表，见 §11.1）
- [x] 账户订阅权益、流量和有效期概览（已接入 `viewerEntitlement`）
- [x] 订单记录页与订单详情页 `/account/orders/{tradeNo}`（已接入后端数据：
  订单列表、支付方式、收银台跳转与取消）
- [x] 账户页订阅凭据：地址不再明文展示，「快速开始使用」区块用「订阅导入」按钮打开
  客户端选择面板（十一种具名客户端，逐个可深链导入、复制专属链接或展开二维码；
  图标取自原版主题，原版没有的 CFW / V2rayN / V2rayNG 取自各自官方仓库；
  不支持当前系统的客户端导入按钮置灰）
- [x] 订阅凭据重置移到登录设备页（与设备撤销同属「撤销已外泄凭据」），
  仪表盘不再承载重置入口
- [x] 放弃「Clash」与「通用订阅」两个入口：前者只能承载旧 Clash 内核的窄协议集
  （无 vless / hysteria / tuic），后者按 UA 猜格式，对新手像一个安全默认值而
  其实不是。QuantumultX 仍缺后端渲染器。V2rayN 只注册了单节点导入协议
  （`v2rayn://{type}/{base64}`），没有订阅 scheme，故只提供复制与二维码。
- [x] 邀请、知识库、流量明细页面骨架（双语空状态，未接后端数据）
- [ ] 其余用户业务页面

## 4. 管理员侧开发

管理员端使用新的 React页面和 Spring Boot实现，但可以保留原版 Xboard管理员
API路径及操作语义。管理员端继续要求独立入口、管理员权限、MFA、CSRF防护和
请求限速。

### 4.1 管理员侧后端

需要实现的接口模块：

- 管理员身份、MFA和设备会话（角色 / 管理员账户管理不做）。
- 用户管理、订阅凭据重置、封禁、邮件和流量重置。
- 套餐、权限组、订单、优惠券和支付配置。
- 服务器、节点、路由组、节点 Token和运行状态。
- 公告、知识库、邮件设置和邮件模板。
- 系统配置和仪表盘。主题与插件扩展不做。

兼容原则：

- 可以保留原版管理员 API分组和操作名称。
- 数据模型、权限检查和业务服务必须使用新系统实现。
- 不复制原版无效接口和控制器缺失问题。
- 不恢复通用管理员请求审计日志。
- 主题和 PHP插件兼容不做，见 §10.2 D。

当前状态：

- [x] 管理员独立登录入口和独立会话安全上下文
- [x] 管理员登录接口、Vite代理与 MFA流程的端到端运行验证
- [x] 管理员 TOTP MFA与一次性恢复码
- [x] 管理员会话（登录 / 刷新 / 登出 / MFA）。设备列表与远程撤销不做，见 §10.2 D
- [x] 系统设置接口骨架 `/api/v2/admin/config/fetch|save` 与测试邮件；
  `email`、`server`、`subscribe_template` 三个分区字段级完整
- [ ] `site`、`safe`、`invite` 三区的字段级落库（清理 A 类后分别只有 4/8、
  7/19、4/13 个字段可读写，其余抛 400 `SETTING_NOT_SUPPORTED`）。逐字段清单见 §10
- [x] 套餐控制面 `/control/catalog/plans`（创建、更新、删除与订阅人数）
- [x] 节点管理 `/api/v2/admin/server/manage`（CRUD、复制、排序、批量、
  ECH 密钥、节点流量重置）
- [x] 机器管理 `/api/v2/admin/server/machine`（令牌、安装命令、节点绑定、
  负载历史）
- [x] 访问组 `/api/v2/admin/server/group` 与路由规则
  `/api/v2/admin/server/route`
- [x] 订单管理 `/api/v2/admin/order`（列表与筛选、标记已支付、取消）
- [x] 支付配置 `/api/v2/admin/payment`（易支付实例、手续费与启用状态）。
  **2026-09-18 决策：只做易支付（EPay）协议，不接原版其余网关**
- [ ] 管理员用户管理（面向终端用户：列表、筛选、详情、封禁 / 解封、
  改流量与有效期、重置订阅凭据）。管理员账户与角色管理不做，见 §10.2 D
- [x] `subscribe_template` 分区后端落库（六个模板键读写 + 保存前校验，见 §5）
- [ ] `subscribe`、`telegram`、`app` 分区后端落库（三分区目前整区 404，见 §10）
- [ ] 邮件模板后端端点 `mail/template/*`（前端契约已就绪，确认要做）
- [ ] 仪表盘统计后端（`AdminDashboardPage` 调用的接口，确认要做）
- [ ] Telegram Webhook 后端端点（前端契约已就绪）
- [ ] 其余管理业务接口

### 4.2 管理员侧前端

需要实现的页面：

- 管理仪表盘。
- 系统、安全、订阅、邀请、节点、邮件和应用设置。
- 管理员 MFA。
- 用户管理、流量重置和用户详情。
- 套餐、权限组、订单、优惠券和支付管理。
- 服务器、节点和路由管理。
- 公告、知识库和邮件模板。

以下不做：管理员与角色、礼品卡、主题配置、插件管理、工单（见 §10.2 D）。

各页面需要统一提供表格、搜索、筛选、分页、表单校验、危险操作确认和错误反馈。

当前状态：

- [x] 管理员 MFA页面
- [x] 管理员首次登录 MFA配置、登录挑战与用户端完全分离
- [x] 首次登录 MFA扫码配置：内置零依赖二维码渲染，兼容 Google Authenticator
  等 TOTP验证器，保留手动输入密钥作为兜底
- [x] 登录设备页面
- [x] 管理员控制台布局、完整模块导航和路由守卫
- [x] 仪表盘裸前端与中英文切换
- [x] 仪表盘筛选、刷新、业务跳转和失败任务分页弹窗
- [x] 仪表盘端点契约与数据类型（前端预留；后端待实现）
- [x] 复刻原版九个设置分区；七项归系统配置，节点配置与订阅模板归节点管理并
  使用独立全宽布局（**UI 复刻完成，字段级接通未完成**：`subscribe`、`telegram`、
  `app` 三区后端未落库，点进去报「配置加载失败」；`site`、`safe`、`invite`
  大部分字段保存报错。见 §10）
- [x] 原版自动保存（按字段防抖、切换分区冲刷、未保存离开提示）、邮件测试交互
- [x] 邮件模板、Telegram Webhook前端交互与 API 契约（后端待实现）
- [x] 套餐管理页（创建、编辑、排序、删除、订阅人数）
- [x] 订单管理页（列表、筛选、标记已支付、取消）
- [x] 支付配置页（易支付实例、手续费与启用状态）
- [x] 节点管理页、机器管理页、访问组页和路由页
- [x] 未开发管理员模块的可导航占位页
- [x] 通用危险操作确认组件（`ConfirmBar`，节点/服务器/套餐页共用，取代原生弹窗）
- [ ] 通用表格、筛选和表单组件（当前各页面各自实现）
- [ ] 其余管理员页面

## 5. 节点与订阅

节点控制面需要兼容现有 `xboard-node`：

- [x] 原版节点 HTTP 协议 `/api/v2/server/config|user|report`（配置、用户
  列表、流量上报计费）
- [x] 旧版 UniProxy 兼容 `/api/v1/server/UniProxy/*`
- [x] xboard-node机器模式：握手、机器节点列表、负载状态上报与轮询间隔下发
- [x] WebSocket `/ws` 实时通道：握手认证（机器/节点/旧版令牌）、配置与用户
  推送、设备状态、访问组用户同步
- [x] 通讯设置热生效：WebSocket 开关拒绝握手并断开全部连接、旧版令牌轮换
  断开非机器连接、拉取/推送间隔下限（30/10 秒）
- [x] 节点管理端到端验收与缺陷修复：停用节点仍接受存量流量上报（避免漏计
  费）、节点掉线时配置变更降级为机器级重新发现、同机器监听端口唯一性校验、
  复制节点自动取空闲端口、权限组用户数与删除保护改用权益口径、面板区分
  「未上报」与「疑似启动失败」
- [x] 管理员节点、机器、访问组和路由管理
- [ ] 已知取舍：删除节点会丢弃该节点最后一个上报周期（默认 60 秒）的流量。
  agent 停止前推送的收尾报告到达时节点记录已删除，后端无法解析其 `groupIds`，
  也就无法校验这台机器有权给哪些用户记账；跳过该校验会让被攻破的机器令牌能
  给任意用户记流量，代价高于这点流量。彻底修复需要软删除（保留行 + `deleted_at`
  并改造全部节点查询），当前按"记录在案"处理。停用节点不受影响，已可正常计费。
- [ ] 用户流量使用记录与自动重置任务（节点流量上报已计入权益；重置档位见 §11.1）

订阅系统需要：

- [x] 不透明且可撤销、可轮换的订阅凭据：`users.subscription_token` 明文存储
  （capability URL，账号页要能随时展示；哈希化会让它变成「重置后只能看一次」），
  注册时用 `SecureRandom` 生成，`rotateSubscriptionCredential` 立即作废旧地址。
  公开入口 `GET /sub/{token}`：令牌只在路径里、不进日志与错误 detail；未知令牌
  与已轮换令牌同为 404（防枚举）；封禁/无有效期权益/流量耗尽为 403 空 body。
- [x] Clash、Clash.Meta、Stash、sing-box与通用 v2ray 链接输出：`flag` 参数优先、
  其次 User-Agent 子串匹配、最后回落通用链接；`types` 收窄协议白名单，
  `filter` 按名称（含即匹配）与标签过滤（>20 码点丢弃）。窄 Clash 只输出
  ss/vmess/trojan/socks/http，Meta 系输出 mihomo 全协议；渲染器与协议字段口径
  逐条对齐原版 `Clash.php` / `ClashMeta.php` / `SingBox.php` / `General.php`。
- [x] Surge与 Surfboard 输出：两者共用 Surge 语法，一个渲染器承载，差异只有
  四处 —— 名称分隔符（`name = ss` 对 `name=ss`）、ss 口令白名单（Surfboard 认识
  `2022-blake3-chacha20-poly1305`）、AnyTLS 是否带 `tfo`/`udp-relay`、响应类型
  （Surge `application/octet-stream`；原版 Surfboard 未设类型，框架填了
  `text/html`，本平台改为 `text/plain`）。五个占位符 `$proxies`、`$proxy_group`、
  `$subs_link`、`$subs_domain`、`$subscribe_info` 与原版同名同义，
  `$subscribe_info` 的换行是字面量 `\n`。
- [x] Stash：Clash 文档形态、mihomo 协议集减去 mieru、字段口径单列一档
  （`Dialect.STASH`）。原版没有 `default.stash.yaml`，其播种回落到 Clash 模板，
  本平台沿用同一回落。
- [x] 管理员可编辑模板：打包 `default.clash.yaml` / `default.clashmeta.yaml` /
  `default.sing-box.json` / `default.surge.conf` / `default.surfboard.conf`，后台
  `subscribe_template` 分区读写，**保存前校验能解析且具备预期顶层键 / 占位符**
  （原版不校验，写坏会让所有用户同时拿到坏配置），清空即回落打包默认值。
  通用 v2ray 链接与原版一致，不是模板驱动。
- [x] 有意偏离原版一处：打包的 Surge 模板注释掉原版写死的
  `external-controller-access = surgepasswd@...`。上游把 HTTP API 的固定口令印在
  每个用户的配置里，等于所有人共用一个口令；默认关闭并注明「要用就自己改密码」。
  Surfboard 模板与原版逐行一致，未改动。
- [x] 自助轮换：账号页展示订阅链接、二维码与「重置订阅链接」
- [ ] 独立订阅域名：后台配置**多个**订阅域名，并按用户确定性轮发（§11.7）。
  当前 `SubscriptionLinkService` 只取第一个域名（有意的临时取舍，理由见 §11.7）
- [ ] 订阅访问限速：每用户每秒最多 2 次请求（§11.4）。异常用量监控不做（§10.2 D）
- [ ] 客户端版本闸门（原版 `protocolRequirements`，如 Stash 用 `9999.0.0` 关掉
  trojan Reality 与 vmess httpupgrade；本轮只做协议白名单，未做版本判定）
- [ ] 未验证：真实客户端（Stash / Surge / Surfboard / Clash Verge / sing-box /
  v2rayN）导入解析。已验证的是输出结构与模板契约，以及 `curl -A` 的三种新格式
  响应头与正文。

## 6. 开发顺序

### 阶段一：用户身份与账户闭环

- [x] 原版 Freedom HTML公共主页与账户入口
- [x] 注册前邮箱验证、Turnstile、IP限流
- [x] 用户与管理员独立登录、退出和令牌刷新
- [x] 邮箱验证和密码找回
- [x] 账户资料、密码修改和设备会话
- [x] 用户身份前后端集成测试

### 阶段二：用户商品与订阅状态

- [x] 套餐展示与价格周期
- [x] 当前订阅、流量和有效期
- [x] 用户订阅凭据与配置输出（Clash / Clash.Meta / Stash / Surge / Surfboard /
  sing-box / 通用 v2ray 链接）

### 阶段三：用户交易与支持

- [x] 下单与订单折抵（含新购/续费/升级/流量重置分类）
- [x] 用户订单详情页、支付方式选择与收银台跳转（`/account/orders/{tradeNo}`）
- [ ] 续费提醒和订单记录页其余数据接入
- [ ] 优惠券、邀请与佣金（邀请后端已完成，页面数据接入待完成；礼品卡不做，
  见 §10.2 D）
- [ ] 公告、知识库和通知（页面骨架已有）

### 阶段四：管理员业务

- [x] 管理员布局、导航和路由守卫
- [x] 仪表盘与系统设置前端骨架
- [x] 管理员 REST客户端（仪表盘、设置、套餐、节点、机器、访问组、路由）
- [ ] 管理员用户管理（P0，见 §10.4；管理员账户与角色管理不做，见 §10.2 D）
- [ ] 通用管理页面组件
- [ ] 用户管理（封禁、改流量与有效期、重置订阅凭据）
- [x] 套餐管理（控制面 + 管理页）
- [ ] 权限组
- [x] 节点流量重置（用户流量重置与自动任务待做）
- [ ] 邮件模板端点 `mail/template/*`
- [ ] 仪表盘统计后端
- [ ] 后台配置项字段级接通（§10：清理后 74 个可编辑字段仅 33 个接通后端）

### 阶段五：节点管理

- [x] 服务器、节点和路由（管理端 + 访问组）
- [x] xboard-node兼容接口（HTTP 协议、机器模式、Legacy UniProxy、
  WebSocket）
- [x] 节点状态和流量上报（负载、在线用户与流量计费）
- [ ] 用户使用记录与自动重置任务

### 阶段六：交易管理

- [x] 订单管理（管理端列表与筛选、标记已支付、取消）
- [x] 支付配置与回调（易支付 EPay：后台配置、手续费、回调开通对账）。
  只做 EPay，不接原版其余网关
- [ ] 优惠券管理页（优惠券表与下单折抵逻辑已实现，仅缺 CRUD 入口，
  用户侧优惠券输入框当前不可用；见 §10.3）

### 阶段七：内容与系统

- [ ] 公告、知识库和邮件（模板端点待实现；`support` 包当前为空）
- [ ] 系统设置与仪表盘（后端全量统计、队列工作负载待实现；系统设置的字段级
  缺口见 §10）

主题与插件扩展不做，见 §10.2 D。

### 阶段八：协议与上线验收

- [ ] 完成用户 API指纹验收
- [ ] HTTP/3、HTTP/2和 HTTP/1.1入口验收
- [ ] 公网测试环境端到端验收

## 7. 当前进度

- [x] Spring Boot Maven工程
- [x] React TypeScript工程
- [x] Docker化 PostgreSQL和 Redis开发环境
- [x] 后端与前端 CI验证
- [x] 身份、令牌、邮件验证和设备会话基础
- [x] 用户密码找回、资料与密码修改
- [x] 邀请码与配置化 SMTP
- [x] 原版 Freedom主页与用户身份账户前端闭环
- [x] 管理员 MFA（含首次配置扫码）
- [x] 删除低价值管理员审计功能
- [x] 完成原版管理员页面、按钮和 API范围调查
- [x] 管理员仪表盘、完整导航和中英文裸前端骨架
- [x] 用户套餐目录与订阅权益前后端闭环
- [x] 管理员套餐、节点、机器、访问组与路由控制面（后端 + 前端页面）
- [x] 易支付（EPay）支付：后台配置、手续费、发起支付与回调开通对账
- [x] 管理员交易控制台：订单管理页与支付配置页（含订阅模板分区）
- [x] 用户下单闭环：套餐结算页、付款周期、优惠券校验、订单折抵、订单记录页与
  订单详情页 `/account/orders/{tradeNo}`
- [x] xboard-node HTTP/轮询/WebSocket 兼容与实时同步
- [x] 节点通讯设置热生效与管理员设置页自动保存重构
- [x] ByteVirt-SG 测试环境：Docker 部署面板（PG/Redis/后端/前端）+
  xboard-node 机器模式端到端（握手、节点发现、配置与用户输出、WebSocket
  实时推送、机器状态上报）
- [x] 订阅凭据与配置输出：`/sub/{token}` 公开入口、七种客户端格式、管理员可编辑
  模板与自助轮换
- [ ] 后台配置项字段级接通（§10 盘点：清理后 74 个可编辑字段仅 33 个接通后端）
- [ ] 管理员完整业务控制台
- [ ] 用户完整业务闭环
- [ ] 公网测试和上线验收

## 8. 计划维护规则

- 每完成一个功能，同步更新对应复选框和当前进度。
- 功能代码与计划更新进入同一次提交。
- 删除或放弃功能时记录原因和数据迁移处理。
- 未通过测试或人工验收的功能不能标记完成。
- 文档不得记录密码、Token、MFA恢复码、私钥或其他凭据。
- `plan.md` 是唯一进度清单，功能提交时必须同步更新，禁止积压后再补。

## 9. 部署

代码由 GitHub Actions验证并构建不可变镜像，测试和生产服务器从 GHCR拉取指定版本部署。
新平台镜像由 `platform-publish` 工作流在 `dev` 分支发布（`xboard-backend`、
`xboard-frontend`），测试环境使用 `compose.test.yml` 部署。

## 10. 后台配置项缺口盘点

> 盘点时间：2026-09-18
>
> 口径：通读 `PlatformConfigurationService.sectionSettings()`（读）与
> `saveSectionSettings()`（写）两个 `switch` 的实际分支，并对全部 84 个字段
> 逐个在 `backend/src/main/java` 内 grep 交叉验证。结论来自源码通读，**不是
> 运行时实测**（本地 8080 有后端，但管理员接口需要鉴权）。

后台设置页在 UI 上完整复刻了原版九个分区，但后端只接通一部分：

- 读：`sectionSettings()` 只认 `site`、`safe`、`invite`、`email`、`server`、
  `subscribe_template` 六个分区，其余抛 404 `SETTINGS_SECTION_NOT_AVAILABLE`。
- 写：`saveSectionSettings()` 按 `分区.字段名` 逐个匹配，未命中抛 400
  `SETTING_NOT_SUPPORTED`。
- 前端不过滤：`systemSettingsSections − {server, subscribe_template}` 七个分区
  全部可导航，管理员点进未落库的分区只会看到「配置加载失败」。

**盘点时合计 84 个可编辑字段，只有 33 个真正接通后端。**

2026-09-18 按 §10.2 A 清理空壳字段后，UI 上还剩 **74 个字段：33 个接通、
41 个空壳**。下表为清理后的现状。

### 10.1 分区体检

| 分区 | UI 字段 | 可读 | 可写 | 结论 |
| --- | ---: | ---: | ---: | --- |
| 站点 `site` | 8 | 4 | 4 | 4 个字段空壳（`logo`、`stop_register`、`try_out_*`） |
| 安全 `safe` | 19 | 7 | 7 | 12 个字段空壳；`captcha_type` 只认 `turnstile` |
| 订阅 `subscribe` | 6 | 0 | 0 | 整区 404 |
| 邀请 `invite` | 13 | 4 | 4 | 9 个字段空壳 |
| 服务器 `server` | 5 | 5 | 5 | 完整 |
| 邮件 `email` | 7 | 7 | 7 | 唯一字段级完整的分区 |
| Telegram `telegram` | 4 | 0 | 0 | 整区 404 |
| 应用 `app` | 6 | 0 | 0 | 整区 404 |
| 订阅模板 `subscribe_template` | 6 | 6 | 6 | 完整 |
| **合计** | **74** | | **33** | **41 个空壳** |

说明：`可读` / `可写` 是后端实际接通的 key 数量，与 UI 字段一一对应（原先多出的
`device_limit_mode` 已按 §10.2 D 删除）。

`PlatformConfigurationServiceTest` 只覆盖 `server` 分区，其余分区没有字段级测试，
这也是缺口长期没被发现的原因。

### 10.2 空壳字段三分类

#### A. 已删除（2026-09-18 执行）

从后台 UI 移除。后端本就没有对应实现，无需数据迁移。

| 字段 | 原因 |
| --- | --- |
| `subscribe_path` | 订阅入口按指纹设计固定为 `/sub/{token}`，不可自定义 |
| `currency`、`currency_symbol` | 新平台价格自带 ISO 币种，前端用 `Intl.NumberFormat` 格式化，比原版全局货币更正确 |
| `secure_path` | **决策：由 Nginx 直接配置，不做成后台选项。** 原版把入口路径做成可配置是为了方便使用者自行定制部署环境；本平台是自用商用系统，部署环境由我们自己掌握，不需要把它暴露成一个可能被改错的管理项 |
| `safe_mode_enable` | 与 `secure_path` 同一决策：域名访问控制属于 Nginx 层职责 |
| `force_https` | 由反代 / TLS 承担 |
| `app_description` | 首页为复刻的静态 Freedom HTML，读不到该配置 |

#### B. 真缺口，需要补实现

| 组 | 字段 | 现状 |
| --- | --- | --- |
| 注册开关 | `stop_register` | 后端完全无实现，关不掉注册 |
| 试用套餐 | `try_out_plan_id`、`try_out_hour` | 后端完全无实现 |
| 限流策略 | `register_limit_*`(3)、`password_limit_*`(3) | 功能已有（`RegistrationVerificationService` 按 IP 计数、`LoginAttemptService`），但由 Spring 配置驱动，后台开关未接线 |
| 验证码 | `recaptcha_*`(5) | UI 提供三个选项，`saveCaptchaType` 只接受 `turnstile`，选 reCAPTCHA 直接 400 |
| 邮箱策略 | `email_gmail_limit_enable` | 无实现。**原版语义**（`RegisterService.php`）：开启后只要邮箱 `@` 前含 `.` 或 `+` 就拒绝注册，用来堵 Gmail 别名批量注册 —— `a+tag@gmail.com`、`a.b@gmail.com` 都投递到同一个邮箱，一个 Gmail 账号可以反复领试用、刷邀请。**原版实现是全局的，会误伤 `first.last@company.com` 这类正常企业邮箱。** 若要补，应只在域名是 `gmail.com` / `googlemail.com` 时校验，且仅拒 `+`（Gmail 会忽略 `.`，但正常企业邮箱的 `.` 有意义），并与已有的邮箱验证、IP 限流叠加 |
| 佣金体系 | `commission_*`(9) | 邀请关系已实现，佣金分发 / 提现 / 三级分销全无 |
| Telegram | `telegram_*`(4) | 整个模块不存在 |
| 应用下载 | `windows_*`、`macos_*`、`android_*`(6) | 整个模块不存在 |
| 邮件事件 | `new_order_event_id`、`renew_order_event_id`、`change_order_event_id` | 下单 / 续费 / 变更不发邮件 |
| 业务开关 | `plan_change_enable`、`surplus_enable`、`reset_traffic_method` | 功能已实现（`SurplusValuation`、套餐升级折抵），但开关不生效 |
| 品牌 | `logo` | 无实现 |

#### C. 反向缺 —— 后端已实现但 UI 没有

无。原本唯一一项 `device_limit_mode` 已于 2026-09-18 删除，决策见 §10.2 D。

背景记录（供日后回溯）：`device_limit_mode` 是「全局设备数限制的宽松模式」开关，
关闭 = 严格（同一 IP 跨多个节点统计为多台设备），开启 = 宽松（仅统计为 1 台）。
它既无法准确反映真实的同时在线设备数，多设备使用也不构成实际漏洞，故不实现。

**在线设备数据的现状**（删除上限后）：
- 面板侧的采集与展示全部保留，只是不再有「上限」这个概念。
- `NodeProtocolService.aliveListLegacy` 原先只把 `device_limit > 0` 的用户放进
  追踪集合，现在改为**该节点服务的全部用户**，否则删掉上限后就没有任何用户会被
  统计。
- WebSocket 设备同步（`NodeWebSocketHandler`）本来就是全量下发，不受影响。
- ⚠️ **旧式 agent 待实测（本平台不涉及）**：V2bX 文档称节点端在「套餐设备数限制
  > 0」时才启用设备统计。本平台现在下发的 `device_limit` 恒为 0（保留该键是为了不改变
  wire 格式），因此**旧式节点（V2bX / XrayR）是否仍会上报在线 IP 需要对着真实 agent
  验证**。
  **但本项目不使用 V2bX / XrayR，只用 xboard-node，已确认不受影响**：
  - `device_limit` 过滤只存在于 `alivelist` 这个 **Legacy UniProxy** GET 端点，
    只有旧式 agent 会调；
  - xboard-node 走 `/api/v2/server/report` + WebSocket。上报侧
    （`NodeProtocolService` 里 `payload.containsKey("alive")` 分支）**从来没有按
    `device_limit` 过滤**；下发侧 `NodeWebSocketHandler.sendDevices` 本来就全量。
  - 结论：xboard-node 的在线设备数据链路与 `device_limit` 无关，删除上限不影响它。
  若日后要接入旧式 agent，再补这条实测。

#### D. 已放弃（2026-09-18 决策）

| 功能 | 决策与理由 |
| --- | --- |
| 订阅头注释 `show_info_to_server_enable` / `show_protocol_to_server_enable` | 不要。订阅配置里不输出站点信息与协议名注释。两字段已从 `subscribe` 分区移除 |
| 工单模块（后台「工单管理」、用户「我的工单」、`ticket_must_wait_reply`、仪表盘「待处理工单」统计） | 不要。工单由后续接入的外部客服系统替代。两侧入口、页面、路由、导航、统计卡与 `ticket_must_wait_reply` 设置均已移除；`SupportWidget` 的「转人工」保留为客服系统的接入位 |
| 后台路径混淆 `secure_path` / 安全模式 `safe_mode_enable` | 见 §10.2 A：由 Nginx 直接配置 |
| 管理员与角色管理 | 不要。系统只保留单一管理员身份，不做角色 / 权限模型，也不做管理员账户 CRUD。管理员账户由部署时直接创建 |
| 管理员「登录设备」页 | 不要。管理员只有一人，没有多设备管理需求。`AdminSessionController` 的登录 / 刷新 / 登出 / MFA 保留，只是不做设备列表与远程撤销 |
| 礼品卡 | 不要。相关表与服务从未实现，无数据迁移 |
| 主题配置 | 不要。用户站是自研 React，不做主题上传 / 切换 |
| 插件管理 | 不要。不做 PHP 插件兼容，也不做 Java 插件扩展机制 |
| 原版其余支付网关（Btcpay / CoinPayments / AlipayF2f 等） | 不要。只做易支付（EPay）协议；BEpusdt 这类共用同协议的网关按需另配一条 EPay 实例即可，无需单独适配 |
| `device_limit_mode` | 不要。两种模式都无法准确判断「同时在线设备数」，且用户多设备访问不构成漏洞。已删除后端常量 `SERVER_DEVICE_LIMIT_MODE_KEY`、`sectionSettings` 回显、`saveSectionSettings` 分支、`NodeCommunicationSettings.deviceLimitMode` 字段与相关测试 |
| 套餐设备数上限 `device_limit` | 不要。面板无法严格控制在多设备上使用订阅（同一个 IP 可能是一个 NAT 出口后面的多人，也可能是同一个人的 IPv6 双栈 / 移动网络切换），既然限不准就不做成配置。已删除 `service_plans.device_limit` 与 `subscription_entitlements.device_limit` 两列（迁移 V21）、域模型字段、`PlanManagementService` 校验、三个 View 字段、GraphQL `deviceLimit` 与全部前端展示与表单。**在线设备数据继续采集与展示**（见 §10.2 C 与 §11.4） |
| 异常用量监控 | 不要。只保留订阅入口的固定速率限制（每用户每秒 2 次），不做用量异常检测与告警 |

> 以上「不要」项已从管理员前端导航移除。除 `device_limit_mode` 外，后端本就没有
> 对应实现，无需删除代码。

### 10.3 后台模块缺口

导航中 5 个占位页，其后端模块同样不存在：

| 导航项 | 后端现状 |
| --- | --- |
| 用户管理 | 无 `AdminUserController`。封禁、改流量与有效期、重置订阅凭据、看用户详情全部无入口 |
| 优惠券管理 | 无 CRUD。`coupons` 表、`CouponEvaluator`、下单折抵均已实现，但没有任何创建入口，只能手写 SQL 造券 —— **用户侧优惠券输入框实际不可用** |
| 公告管理 | 无 |
| 知识库管理 | `support` 包为空 `package-info.java` |
| 流量重置记录 | 无 |

其他后端端点缺口（均已确认要做）：

- 邮件模板 `mail/template/*`（前端交互与契约已就绪）
- 仪表盘统计后端（`AdminDashboardPage` 调用的接口全部是前端预留）
- Telegram Webhook 端点

### 10.4 待办清单

按「挡住商业化」排序。每完成一项，勾掉并把对应改动写入同一次提交。

> P0–P3 汇总全平台待办，含 §3 用户侧与 §5 订阅与节点的明细项；那两节的复选框
> 是分模块的详细视图，本清单是优先级视图，两处同步维护。

#### P0

- [ ] 管理员用户管理：列表、筛选、详情、封禁 / 解封、改流量与有效期、
  重置订阅凭据、重置密码、发送邮件。**完整接口与字段规格见 §11.8**

#### P1

- [x] 清理 §10.2 A 类空壳字段（2026-09-18 完成：UI 从 84 字段减到 74）
- [ ] 优惠券管理 CRUD（后端 + 页面），让已实现的折抵链路真正可用
- [ ] 安全开关接线：`stop_register`、`register_limit_*`、`password_limit_*`；
  `captcha_type` 要么实现 reCAPTCHA，要么从 UI 移除选项
- [ ] `email_gmail_limit_enable`：按 §10.2 B 的正确语义实现。2026-09-18 决策：
  仅当域名是 `gmail.com` / `googlemail.com` 时校验，且只拒 `+`、不拒 `.`
  （企业邮箱的 `.` 有意义），叠加已有的邮箱验证与 IP 限流
- [ ] 邮件模板端点 `mail/template/*`（前端交互与契约已就绪）
- [ ] 订单事件邮件（`new_order_event_id` / `renew_order_event_id` /
  `change_order_event_id`）
- [ ] 续费与流量提醒：`remind_mail_enable` 目前只存储不消费；补
  `default_remind_expire`、`default_remind_traffic` 及定时任务

#### P2

- [ ] 仪表盘统计后端（收入、用户、流量、节点排名、失败任务）
- [ ] 公告、知识库两个模块（后端 + 页面，用户侧骨架已在）
- [ ] 接入外部客服系统，替换已放弃的工单模块（用户侧 `SupportWidget` 的
  「转人工」为接入位）
- [ ] 佣金体系 `commission_*`(9) 与提现
- [ ] 试用套餐：按 §11.1，除 `try_out_plan_id` / `try_out_hour` 外还需扩展
  「试用流量」「试用带宽」两个配置项
- [ ] 订阅业务开关接线：`plan_change_enable`、`surplus_enable`、`reset_traffic_method`
- [ ] 用户流量自动重置定时任务（实现 §11.1 的五档 `reset_policy`；周期订阅默认
  按月、流量包不重置）与流量重置记录页

#### P3

- [ ] `telegram`、`app` 两个分区字段级落库
- [ ] Telegram Webhook 端点
- [ ] 品牌 `logo`
- [ ] 应用下载配置 `windows_*` / `macos_*` / `android_*`

#### 用户侧（§3 明细）

- [ ] 订阅分类导航：全部 / 按周期 / 按流量三个页签（原版用户站有，当前是平铺列表）
- [ ] 知识库页面接后端（骨架已在）
- [ ] 流量明细页面接后端（骨架已在）
- [ ] 邀请与佣金页面接后端（邀请后端已完成，只差数据接入）
- [ ] 订单记录页其余数据接入
- [ ] 续费提醒
- [ ] 站内通知中心

#### 订阅与节点（§5 明细）

- [ ] 独立订阅域名：后台配置多个订阅域名 + 按用户确定性轮发（见 §11.7）
- [ ] 订阅接口访问限速：每用户每秒最多 2 次请求（§11.4）
- [ ] 客户端版本闸门（原版 `protocolRequirements`，如 Stash 用 `9999.0.0`
  关掉 trojan Reality 与 vmess httpupgrade）
- [ ] 真实客户端导入验证（Stash / Surge / Surfboard / Clash Verge / sing-box /
  v2rayN；目前只验过输出结构与 `curl -A` 响应）
- [ ] 删除节点丢失最后一个上报周期流量的问题（需节点软删除，见 §5）

## 11. 需求确认（2026-09-18）

> 与产品方逐条确认过的业务口径。实现时以本节为准；与 §10 待办清单冲突时，
> 本节的口径优先。

### 11.1 商业模型

- **一次性流量包：要上。** 后端 `PlanType.TRAFFIC_PACKAGE` 与用户侧渲染都已存在
  （`PlansPage` 会区分展示一次性/周期），**缺的是订阅分类导航** —— 原版用户站有
  「全部 / 按周期 / 按流量」三个页签，本平台目前是平铺列表。
- **注册试用：要。** 管理员后台需要一组特殊配置：试用**套餐**、试用**时长**、
  **流量**、**带宽**。对应 §10.2 B 的 `try_out_plan_id` / `try_out_hour`，并按
  本条再扩展出「试用流量」「试用带宽」两个配置项。
- **升级折抵：按原版剩余价值抵。** 后端 `SurplusValuation` 已是这个口径，沿用。
- **流量重置档位（2026-09-18 定档）**：
  - **周期订阅 → 按月 `MONTHLY_FROM_ACTIVATION`**：从开通日起每满一个月重置。
    套餐编辑页的默认值已经是这一档。
  - **流量包 → 不重置 `NEVER`**：已由 `PlanManagementService` 强制
    （`planType == TRAFFIC_PACKAGE` 一律归一为 `NEVER`）。

#### 流量重置五档语义（备查）

套餐的 `reset_policy` 有五个取值；`reset_traffic_method` 是全局默认、可按套餐覆盖
（与原版一致）。分水岭是**日历对齐**还是**激活日对齐**：

| 档位 | 语义 | 适用场景 | 代价 |
| --- | --- | --- | --- |
| 每月 1 号 `FIRST_DAY_OF_MONTH` | 所有用户在日历月初同时重置 | 主流机场默认。运营好做账，流量高峰可预测 | **月底购买的用户几天后就被重置**，感知不公平 |
| 按月 `MONTHLY_FROM_ACTIVATION` ← **周期订阅选用** | 从开通日起每满一个月重置 | 用户公平，买满一整周期 | 重置日散布全月，看不出月初高峰；需按用户记录上次重置时间 |
| 不重置 `NEVER` ← **流量包选用** | 整个周期只给一次流量 | 一次性流量包；或不想让用户月底突击下载 | 用完即止，只能靠加购流量包续命 |
| 每年 1 月 1 号 `FIRST_DAY_OF_YEAR` | 年付套餐在日历年初重置 | 年付为主时与月度同口径 | 「每月 1 号」的年化版本，同样有年底购买不划算的问题 |
| 按年 `YEARLY_FROM_ACTIVATION` | 年付套餐从开通日起满一年重置 | 年付且追求公平 | 「按月」的年化版本 |

自动重置定时任务尚未实现，见 §10.4。

### 11.2 支付

- 只接易支付（EPay）协议。
- **不做系统退款**，退款在客服系统私聊处理。
- 币种只用 **CNY**。

### 11.3 增长与营销

- **优惠券：照搬原版 Xboard 逻辑**（折扣类型、适用范围、有效期、每人限用次数）。
  目前只缺管理端 CRUD，用户侧校验与折抵已实现。
- **礼品卡：不做**（见 §10.2 D）。
- **返佣：除提现方式外照搬原版业务逻辑**；提现不实现，在客服系统私聊处理。
  原版口径：全局默认比例 + 用户级覆盖、仅首次支付产生佣金（可关）、
  订单完成 3 日后自动确认（可关）、三级分销（可关）。
- **知识库：自建文档系统**，参考原版。

### 11.4 风控与合规

- **封号语义：禁止登录 + 断开订阅，两者都要。** 封禁后面板登录被拒，订阅入口
  同时停止下发配置。
- **订阅链接限速：每用户每秒最多 2 次请求。**
- **不做全局设备数限制模式（`device_limit_mode`），也不做套餐级设备数上限
  （`device_limit`）**：两种口径都无法准确判断真实的同时在线设备数（同一 IP 可能是
  NAT 出口后的多人，也可能是同一个人的双栈 / 移动网络切换），既然限不准就不做成配置。
  两处均已删除（见 §10.2 D）。
- **在线设备数据照常采集与展示**：管理员仍能看到每个账号当前有多少个在线设备。
  这只是观测，不再有上限。删除上限后 `aliveListLegacy` 已改为追踪该节点的全部用户；
  本项目只用 xboard-node，其数据链路与 `device_limit` 无关（见 §10.2 C）。
- **不做异常用量监控**：只做订阅入口限速（每用户每秒 2 次），不做用量异常检测与告警。
- **不做实名、不做内容审计。**

### 11.5 客服

最后实现，届时再定方案。用户侧 `SupportWidget` 已经是接入位。

### 11.6 管理端

- **管理员只有一人，不做分级、不做角色模型。**
- **不做管理员「登录设备」页**（列出登录设备 + 远程撤销）：单人使用，无此需求。
- **用户管理与原版 Xboard 保持一致**，接口与字段清单见 §11.8。
- **仪表盘沿用现有管理员面板的布局**，后续接入真实数据即可。

### 11.7 交付与多语言

- **独立订阅域名：要做**，与管理后台的多个订阅域名配置一致。
- 中英双语足够。
- 邮件模板复刻原版。

> **注意（订阅域名轮发）**：`SubscriptionLinkService` 当前**只取配置列表的第一个**
> 域名，代码注释给的理由是「用户两次复制必须拿到同一个链接」。原版是每次请求
> 随机挑一个，副作用是链接会漂移。既要多域名负载分散、又不要链接漂移，正确做法
> 是**按用户确定性取模**（用 `subscription_token` 的哈希对域名数量取模），对同一
> 用户永远稳定、对不同用户均匀分散。建议采用后者。

### 11.8 管理员用户管理（P0 规格）

按原版 `AdminRoute.php` 的 `user` 分组与 `UserUpdate.php` 校验规则对齐。

接口：

| 接口 | 用途 |
| --- | --- |
| `user/fetch` | 列表 + 筛选 + 分页 |
| `user/update` | 编辑 |
| `user/getUserInfoById` | 详情 |
| `user/generate` | 创建用户 |
| `user/dumpCSV` | 导出 CSV |
| `user/sendMail` | 给用户发邮件 |
| `user/ban` | 封禁 / 解封 |
| `user/resetSecret` | 重置 UUID 及订阅 URL |
| `user/setInviteUser` | 指定邀请人 |
| `user/destroy` | 删除 |
| `traffic-reset/user/{userId}/history` | 该用户流量重置历史 |
| `traffic-reset/reset-user` | 重置流量 |

可编辑字段：`email`、`password`、`transfer_enable`、`expired_at`、`banned`、
`plan_id`、`commission_rate`、`discount`、`u`、`d`、`balance`、`commission_type`、
`commission_balance`、`remarks`、`speed_limit`。

原版的 `is_admin` / `is_staff` **不做** —— 管理员只有一人（§11.6）。
原版的 `device_limit` **不做** —— 上限已整体移除（§10.2 D）。

列表列：ID、邮箱、在线设备、状态、订阅、权限组、已用流量、总流量、到期时间、操作。
其中「在线设备」是**只读观测数据**，不再有 `/上限` 的写法。

行操作菜单：编辑、分配订单、复制订阅 URL、重置 UUID 及订阅 URL、TA 的订单、
TA 的邀请、TA 的流量记录、重置流量、删除。

页面工具条：创建用户、搜索用户邮箱、高级筛选、导出。
