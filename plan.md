# SinX Platform 开发计划

> 当前分支：`dev`
>
> 更新时间：2026-08-20（同步至 `dev` 分支最新提交）

## 1. 目标

使用 Java 21、Spring Boot、React、PostgreSQL 和 Redis重新实现 Xboard的核心
业务能力，不继承 Laravel代码、旧数据库、用户页面和用户 API。

核心目标：

- 建立独立的用户网站、用户 API和前端资源体系，消除 Xboard默认应用指纹。
- 重新实现用户、套餐、订单、支付、订阅、节点、流量和支持系统。
- 管理员端参考原版 Xboard的功能，允许保留管理员 API路径和操作语义。
- 节点控制面兼容现有 `xboard-node`，用户代理流量仍直接访问节点 VPS。
- 用户站点支持 HTTP/3、HTTP/2和 HTTP/1.1协商与回退。

## 2. 总体架构

```text
                         Internet
                            │
          ┌─────────────────┴─────────────────┐
          │                                   │
       用户域名                            管理员域名
          │                                   │
     用户 React前端                      管理员 React前端
          │                                   │
     用户侧后端 API                      管理员侧后端 API
          └─────────────────┬─────────────────┘
                            │
                 Spring Boot模块化单体
                            │
        ┌───────────────────┼───────────────────┐
        │                   │                   │
    PostgreSQL            Redis             后台任务

订阅客户端 ─────→ 用户订阅接口 ─────→ 配置输出

xboard-node ─────→ 节点控制 HTTP API / WebSocket

代理客户端 ─────→ 节点 VPS代理端口 ─────→ Internet
                 不经过网站和面板
```

系统分为四个边界：

- 用户侧：公网用户页面和业务接口，必须使用全新的协议与资源特征。
- 管理员侧：仅管理员访问，优先复刻 Xboard已有管理能力。
- 订阅侧：使用独立域名和不透明凭据返回客户端配置。
- 节点侧：控制数据连接面板，用户代理数据直接经过节点 VPS。

## 3. 用户侧开发

### 3.1 用户侧后端

需要实现的接口领域：

- 身份：注册、登录、退出、令牌刷新、邮件验证、密码重置。
- 账户：用户资料、设备会话、安全设置、订阅状态。
- 商品：套餐查询、价格周期、试用和套餐变更规则。
- 交易：订单创建、支付、取消、续费和支付回调。
- 营销：优惠券、礼品卡、邀请关系和佣金。
- 订阅：订阅凭据、客户端识别、配置输出和凭据重置。
- 支持：公告、知识库、工单和通知。
- 用量：流量、有效期、设备限制和使用记录。

已经完成：

- [x] 注册前邮箱验证码、Cloudflare Turnstile和单 IP限流
- [x] 注册、登录、刷新和退出后端流程
- [x] 新注册邮箱在写库前完成验证
- [x] 密码找回、密码修改和账户资料
- [x] Access/Refresh Token轮换与重放检测
- [x] 设备会话管理
- [x] USER/ADMIN独立会话、Token audience、Refresh Cookie和有效期
- [x] 邀请码生成、查询、注册填写和邀请人关系（策略由管理员配置）
- [x] 套餐目录、价格周期、容量和可售状态查询
- [x] 当前订阅权益、流量用量、有效期和限制查询

仍需完成：

- [ ] 订单、支付和营销领域（`order` 模块当前为空壳）
- [ ] 订阅凭据与配置输出
- [ ] 公告、知识库、工单和通知后端
- [ ] 用户流量使用记录与自动重置任务（节点流量上报已接入权益计费，见 §5）

### 3.2 用户 API 指纹设计

用户侧是应用指纹重设计的重点，不能保留以下 Xboard默认特征：

- `/api/v1/*`、`/api/v2/*`和 `/s/{token}`等路径模式。
- `status/message/data/error`等固定响应包装。
- Xboard错误码、分页字段、请求参数和客户端识别方式。
- Laravel、Nginx、Spring Boot或其他框架默认响应头和错误页面。
- 原版静态资源、文件名称、页面结构、文案和资源哈希。

新用户接口按使用场景拆分：

- 浏览器业务查询和修改使用新的 GraphQL契约。
- 登录、刷新、退出和安全操作使用独立会话接口。
- 支付回调使用独立且可验证的 Webhook接口。
- 订阅使用独立域名、不透明凭据和新的入口格式。
- 文件下载使用独立文件接口。

指纹控制要求：

- 用户接口拥有自己的字段、错误结构、分页方式和状态语义。
- 生产环境隐藏框架版本、默认错误页和非必要响应头。
- 前端只调用用户域名允许的接口，不复用管理员或节点接口。
- API、静态资源、订阅和节点控制使用不同的安全策略与限流规则。
- 保持协议稳定和可维护，不通过频繁随机修改接口制造兼容问题。

已按此落地的实现边界：

- 用户业务查询走 `POST /gateway` GraphQL（`offerCatalog`、`viewer`、
  `viewerEntitlement`、`deviceSessions`、`revokeDeviceSession`）。
- 登录、刷新、退出和安全操作走 `/session/*` 独立会话接口。
- 管理员控制面沿用 `/api/v2/admin/*` 与 `/control/*`，节点兼容层沿用
  `/api/v2/server`、`/api/v1/server/UniProxy` 与 `/ws`，不与用户接口混用。
- 隐藏框架版本、默认错误页和非必要响应头属上线验收项（见阶段八）。

### 3.3 用户侧前端

需要实现的页面：

- 注册、登录、邮件验证和密码重置。
- 用户首页、订阅状态和流量信息。
- 套餐、下单、支付和订单记录。
- 订阅获取、客户端下载和使用引导。
- 个人资料、安全设置和登录设备。
- 优惠券、礼品卡、邀请与佣金。
- 公告、知识库和工单。

当前状态：

- [x] 直接复用原版 Freedom HTML及其静态资源作为公共主页
- [x] 注册页面
- [x] 注册邮箱验证码、Turnstile二次校验和安全状态反馈
- [x] 登录页面
- [x] 注册前邮箱验证闭环（写库前完成验证；无独立“确认页面”路由）
- [x] 密码找回与重置页面
- [x] 账户概览与资料修改页面
- [x] 登录密码修改页面
- [x] 登录设备页面
- [x] 页面切换保持登录状态
- [x] 套餐展示与价格周期页面（已接入 `offerCatalog`）
- [x] 账户订阅权益、流量和有效期概览（已接入 `viewerEntitlement`）
- [x] 订单、邀请、工单、知识库、流量明细页面骨架（双语空状态，未接后端数据）
- [ ] 其余用户业务页面

## 4. 管理员侧开发

管理员端使用新的 React页面和 Spring Boot实现，但可以保留原版 Xboard管理员
API路径及操作语义。管理员端继续要求独立入口、管理员权限、MFA、CSRF防护和
请求限速。

### 4.1 管理员侧后端

需要实现的接口模块：

- 管理员身份、角色、MFA和设备会话。
- 用户管理、订阅凭据重置、封禁、邮件和流量重置。
- 套餐、权限组、订单、优惠券、礼品卡和支付配置。
- 服务器、节点、路由组、节点 Token和运行状态。
- 公告、知识库、工单、邮件设置和邮件模板。
- 系统配置、仪表盘、主题和插件扩展。

兼容原则：

- 可以保留原版管理员 API分组和操作名称。
- 数据模型、权限检查和业务服务必须使用新系统实现。
- 不复制原版无效接口和控制器缺失问题。
- 不恢复通用管理员请求审计日志。
- 主题和 PHP插件兼容放到最后，按 Java/React扩展机制重新设计。

当前状态：

- [x] 管理员独立登录入口和独立会话安全上下文
- [x] 管理员登录接口、Vite代理与 MFA流程的端到端运行验证
- [x] 管理员 TOTP MFA与一次性恢复码
- [x] 管理员设备会话
- [x] 系统设置读写：`site`、`safe`、`invite`、`email`、`server` 五分区
  （`/api/v2/admin/config/fetch|save`）与测试邮件
- [x] 套餐控制面 `/control/catalog/plans`（创建、更新、删除与订阅人数）
- [x] 节点管理 `/api/v2/admin/server/manage`（CRUD、复制、排序、批量、
  ECH 密钥、节点流量重置）
- [x] 机器管理 `/api/v2/admin/server/machine`（令牌、安装命令、节点绑定、
  负载历史）
- [x] 访问组 `/api/v2/admin/server/group` 与路由规则
  `/api/v2/admin/server/route`
- [ ] 管理员用户与角色管理
- [ ] `subscribe`、`telegram`、`app`、`subscribe_template` 分区后端落库
- [ ] Telegram Webhook与邮件模板后端端点（前端契约已就绪）
- [ ] 其余管理业务接口

### 4.2 管理员侧前端

需要实现的页面：

- 管理仪表盘。
- 系统、安全、订阅、邀请、节点、邮件和应用设置。
- 管理员用户、角色、MFA和登录设备。
- 用户管理、流量重置和用户详情。
- 套餐、权限组、订单、优惠券、礼品卡和支付管理。
- 服务器、节点和路由管理。
- 公告、知识库、工单和邮件模板。
- 主题与插件管理。

各页面需要统一提供表格、搜索、筛选、分页、表单校验、危险操作确认和错误反馈。

当前状态：

- [x] 管理员 MFA页面
- [x] 管理员首次登录 MFA配置、登录挑战与用户端完全分离
- [x] 首次登录 MFA扫码配置：内置零依赖二维码渲染，兼容 Google Authenticator
  等 TOTP验证器，保留手动输入密钥作为兜底
- [x] 登录设备页面
- [x] 管理员控制台布局、完整模块导航和路由守卫
- [x] 仪表盘裸前端与中英文切换
- [x] 仪表盘筛选、刷新、业务跳转和失败任务分页弹窗
- [x] 仪表盘端点契约与数据类型（前端预留；后端待实现）
- [x] 复刻原版九个设置分区；七项归系统配置，节点配置与订阅模板归节点管理并
  使用独立全宽布局
- [x] 原版自动保存（按字段防抖、切换分区冲刷、未保存离开提示）、邮件测试交互
- [x] 邮件模板、Telegram Webhook前端交互与 API 契约（后端待实现）
- [x] 套餐管理页（创建、编辑、排序、删除、订阅人数）
- [x] 节点管理页、机器管理页、访问组页和路由页
- [x] 未开发管理员模块的可导航占位页
- [x] 通用危险操作确认组件（`ConfirmBar`，节点/服务器/套餐页共用，取代原生弹窗）
- [ ] 通用表格、筛选和表单组件（当前各页面各自实现）
- [ ] 其余管理员页面

## 5. 节点与订阅

节点控制面需要兼容现有 `xboard-node`：

- [x] 原版节点 HTTP 协议 `/api/v2/server/config|user|report`（配置、用户
  列表、流量上报计费）
- [x] 旧版 UniProxy 兼容 `/api/v1/server/UniProxy/*`
- [x] xboard-node机器模式：握手、机器节点列表、负载状态上报与轮询间隔下发
- [x] WebSocket `/ws` 实时通道：握手认证（机器/节点/旧版令牌）、配置与用户
  推送、设备状态、访问组用户同步
- [x] 通讯设置热生效：WebSocket 开关拒绝握手并断开全部连接、旧版令牌轮换
  断开非机器连接、拉取/推送间隔下限（30/10 秒）
- [x] 节点管理端到端验收与缺陷修复：停用节点仍接受存量流量上报（避免漏计
  费）、节点掉线时配置变更降级为机器级重新发现、同机器监听端口唯一性校验、
  复制节点自动取空闲端口、权限组用户数与删除保护改用权益口径、面板区分
  「未上报」与「疑似启动失败」
- [x] 管理员节点、机器、访问组和路由管理
- [ ] 已知取舍：删除节点会丢弃该节点最后一个上报周期（默认 60 秒）的流量。
  agent 停止前推送的收尾报告到达时节点记录已删除，后端无法解析其 `groupIds`，
  也就无法校验这台机器有权给哪些用户记账；跳过该校验会让被攻破的机器令牌能
  给任意用户记流量，代价高于这点流量。彻底修复需要软删除（保留行 + `deleted_at`
  并改造全部节点查询），当前按"记录在案"处理。停用节点不受影响，已可正常计费。
- [ ] 用户流量使用记录与自动重置任务（节点流量上报已计入权益）

订阅系统需要：

- [ ] 独立订阅域名
- [ ] 不透明且可撤销、可轮换的订阅凭据
- [ ] Sing-box和 Clash等输出适配器
- [ ] 访问限速、异常用量监控和凭据重置

## 6. 开发顺序

### 阶段一：用户身份与账户闭环

- [x] 原版 Freedom HTML公共主页与账户入口
- [x] 注册前邮箱验证、Turnstile、IP限流
- [x] 用户与管理员独立登录、退出和令牌刷新
- [x] 邮箱验证和密码找回
- [x] 账户资料、密码修改和设备会话
- [x] 用户身份前后端集成测试

### 阶段二：用户商品与订阅状态

- [x] 套餐展示与价格周期
- [x] 当前订阅、流量和有效期
- [ ] 用户订阅凭据与配置输出

### 阶段三：用户交易与支持

- [ ] 下单、支付、续费和订单记录（页面骨架已有）
- [ ] 优惠券、礼品卡、邀请与佣金（邀请后端已完成，页面数据接入待完成）
- [ ] 公告、知识库、工单和通知（页面骨架已有）

### 阶段四：管理员业务

- [x] 管理员布局、导航和路由守卫
- [x] 仪表盘与系统设置前端骨架
- [x] 管理员 REST客户端（仪表盘、设置、套餐、节点、机器、访问组、路由）
- [ ] 管理员用户与角色管理
- [ ] 通用管理页面组件
- [ ] 用户管理
- [x] 套餐管理（控制面 + 管理页）
- [ ] 权限组
- [x] 节点流量重置（用户流量重置与自动任务待做）

### 阶段五：节点管理

- [x] 服务器、节点和路由（管理端 + 访问组）
- [x] xboard-node兼容接口（HTTP 协议、机器模式、Legacy UniProxy、
  WebSocket）
- [x] 节点状态和流量上报（负载、在线用户与流量计费）
- [ ] 用户使用记录与自动重置任务

### 阶段六：交易管理

- [ ] 订单、优惠券和礼品卡
- [ ] 支付配置与回调

### 阶段七：内容与系统

- [ ] 公告、知识库、工单和邮件（模板端点待实现）
- [ ] 系统设置与仪表盘（后端全量统计、队列工作负载待实现）
- [ ] 主题和插件扩展

### 阶段八：协议与上线验收

- [ ] 完成用户 API指纹验收
- [ ] HTTP/3、HTTP/2和 HTTP/1.1入口验收
- [ ] 公网测试环境端到端验收

## 7. 当前进度

- [x] Spring Boot Maven工程
- [x] React TypeScript工程
- [x] Docker化 PostgreSQL和 Redis开发环境
- [x] 后端与前端 CI验证
- [x] 身份、令牌、邮件验证和设备会话基础
- [x] 用户密码找回、资料与密码修改
- [x] 邀请码与配置化 SMTP
- [x] 原版 Freedom主页与用户身份账户前端闭环
- [x] 管理员 MFA（含首次配置扫码）
- [x] 删除低价值管理员审计功能
- [x] 完成原版管理员页面、按钮和 API范围调查
- [x] 管理员仪表盘、完整导航和中英文裸前端骨架
- [x] 用户套餐目录与订阅权益前后端闭环
- [x] 管理员套餐、节点、机器、访问组与路由控制面（后端 + 前端页面）
- [x] xboard-node HTTP/轮询/WebSocket 兼容与实时同步
- [x] 节点通讯设置热生效与管理员设置页自动保存重构
- [x] ByteVirt-SG 测试环境：Docker 部署面板（PG/Redis/后端/前端）+
  xboard-node 机器模式端到端（握手、节点发现、配置与用户输出、WebSocket
  实时推送、机器状态上报）
- [ ] 管理员完整业务控制台
- [ ] 用户完整业务闭环
- [ ] 订阅凭据与配置输出
- [ ] 公网测试和上线验收

## 8. 计划维护规则

- 每完成一个功能，同步更新对应复选框和当前进度。
- 功能代码与计划更新进入同一次提交。
- 删除或放弃功能时记录原因和数据迁移处理。
- 未通过测试或人工验收的功能不能标记完成。
- 文档不得记录密码、Token、MFA恢复码、私钥或其他凭据。
- `plan.md` 是唯一进度清单，功能提交时必须同步更新，禁止积压后再补。

## 9. 部署

代码由 GitHub Actions验证并构建不可变镜像，测试和生产服务器从 GHCR拉取指定版本部署。
新平台镜像由 `platform-publish` 工作流在 `dev` 分支发布（`xboard-backend`、
`xboard-frontend`），测试环境使用 `compose.test.yml` 部署。
